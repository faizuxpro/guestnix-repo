import type { TemplateConfig } from "../sunset-lakehouse/index";

const config: TemplateConfig = {
  id: "modern-minimal",
  name: "Modern Minimal",
  description: "Clean, minimalist design for urban stays and modern apartments",
  thumbnail: "/templates/modern-minimal.png",
  tier: "free",
  fonts: ["Inter"],
  defaultColors: {
    primary: "#1a1a1a",
    secondary: "#f5f5f5",
  },
};

export default config;
